import type { ToggleButtonProps } from '../ToggleButton';
export type TabProps = ToggleButtonProps;
/**
 * Renders a tab, and inherits from its parent state and stylistic props.
 *
 * Should be used in a TabList.
 */
export declare const Tab: import("../../utils/forwardRef").FunctionComponentWithAs<import("../../utils/forwardRef").FunctionComponentWithAs<"button", import("../Button").ButtonProps>, ToggleButtonProps>;
export default Tab;
